kind:       ConfigMap
apiVersion: v1

metadata:
  name:      argocd-cm
  namespace: argo-cd
  labels:
    app.kubernetes.io/name:    argocd-cm
    app.kubernetes.io/part-of: argo-cd

data:
  accounts.lunics: apiKey, login
  accounts.lunics.enabled: "true"
